doctype html
html(lang="en")
  head
    meta(charset="UTF-8")
    meta(name="viewport" content="width=device-width, initial-scale=1.0")
    title Password Reset Success
    link(
      rel="stylesheet",
      href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.3/dist/css/bootstrap.min.css"
    )
    style.
      body {
        background-color: #f8f9fa;
        display: flex;
        justify-content: center;
        align-items: center;
        min-height: 100vh;
      }
      .container {
        width: 50%;
      }
      .card {
        width: 100%;
        padding: 2rem;
        border-radius: 1rem;
        box-shadow: 0 0 20px rgba(0,0,0,0.1);
      }

  body
    div.container
      div.card.text-center
        h1.text-success Password Reset Successful!
        p.mt-3 Your password has been successfully changed.
        p.text-muted You can now log in using your new password.
        a.btn.btn-primary.mt-4(href="http://localhost:5173/login") Go to Login